# Gemini API key for the Blockly automation extension.
# Get one at https://aistudio.google.com/app/apikey
# Paste this value into the popup's API Key field — it is saved automatically.
# For build-time injection, set this variable and wire it into popup.js via your bundler.
GEMINI_API_KEY=your_gemini_api_key_here
